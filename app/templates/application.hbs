<div class="container">
  <h1>Amazing Login App</h1>
  <div class="flash-container">
    {{#each flashMessages.queue as |flash|}}
      {{flash-message flash=flash messageStyle='foundation'}}
    {{/each}}
  </div>

  <div class="content">
    {{outlet}}
  </div>
</div>
